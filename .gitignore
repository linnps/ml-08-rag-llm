# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
.env

# Vector store / embeddings cache (regenerated by train.py)
chroma_db/
faiss_index/
embeddings/
*.faiss

# Generated corpus assets (chunks, intermediate)
data/
*.parquet
*.h5
*.pkl
*.npy
*.npz
*.jsonl

# Jupyter
.ipynb_checkpoints/

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store

# Journals (per ~/.claude/CLAUDE.md — never commit raw command output)
journal-*.md

# Secrets / API keys
.env.local
*.key
secrets.json

# But DO commit dashboard figures and final metrics — they're the showcase.
!assets/
!assets/*.png
!results/
!results/metrics.json
